from django.apps import AppConfig


class QuestionsConfig(AppConfig):
    """App config for the questions app: per-quest submission questions and their answers."""
    name = 'questions'
    verbose_name = 'Submission Questions'
