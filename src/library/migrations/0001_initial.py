# Generated by Django 5.2.17 on 2026-08-14 04:59

from django.db import migrations, models


class Migration(migrations.Migration):

    initial = True

    dependencies = []

    operations = [
        migrations.CreateModel(
            name="ContentOrigin",
            fields=[
                (
                    "id",
                    models.BigAutoField(
                        auto_created=True,
                        primary_key=True,
                        serialize=False,
                        verbose_name="ID",
                    ),
                ),
                (
                    "import_id",
                    models.UUIDField(
                        help_text="The import_id of the Library quest or campaign this describes."
                    ),
                ),
                (
                    "content_type",
                    models.CharField(
                        choices=[("quest", "Quest"), ("campaign", "Campaign")],
                        help_text="Whether the import_id names a quest or a campaign. A quest and a campaign could in principle carry the same import_id, so this is part of the identity.",
                        max_length=10,
                    ),
                ),
                (
                    "deck_name",
                    models.CharField(
                        help_text="Name of the deck the content was shared from, as it was at the time of sharing.",
                        max_length=100,
                    ),
                ),
                (
                    "deck_url",
                    models.URLField(
                        help_text="Root URL of the deck the content was shared from, so the attribution can link back."
                    ),
                ),
                (
                    "shared_by",
                    models.CharField(
                        help_text="Username of the person who shared the content, on their own deck. Stored as text rather than a User foreign key: the sharer is a user of another deck's schema, so there is no row here to point at.",
                        max_length=150,
                    ),
                ),
                (
                    "shared_on",
                    models.DateTimeField(
                        auto_now_add=True,
                        help_text="When the content was shared to the Library.",
                    ),
                ),
            ],
            options={
                "verbose_name": "content origin",
                "constraints": [
                    models.UniqueConstraint(
                        fields=("import_id", "content_type"),
                        name="unique_origin_per_library_content",
                    )
                ],
            },
        ),
    ]
