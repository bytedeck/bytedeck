language: python
python:
 - "3.6"

services:
 - docker

before_script: pip install docker-compose --no-cache-dir

script: 
 - docker-compose -f docker-compose.yml run web sh -c "python src/manage.py test src && flake8 src"
